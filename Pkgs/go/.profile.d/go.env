#!/usr/bin/env bash

# Go path setup
case ":${PATH}:" in
    *:"$HOME/.local/go":*)
        ;;
    *)
        # Prepending path in case a system-installed go toolchain needs to be overriden
        export PATH="$HOME/.local/go/bin:$PATH"
        ;;
esac

